__pycache__/
*.pyc
*.pyo
racebox_auth.json
*.dll
.claude/

# FFmpeg binaries (copied locally for bundling — not committed)
ffmpeg.exe
ffprobe.exe

# Build / distribution
build/
dist/
*.egg-info/

# Node
node_modules/

# Auto-generated PyInstaller spec (not the real one)
pyproject.spec

# Logs
*.log

# Internal tooling / local data processing sandbox
test_data_process/

# Product track configs:
# keep template docs in repo, keep real customer/track json local only.
tracks/*.json
!tracks/*.template.json
